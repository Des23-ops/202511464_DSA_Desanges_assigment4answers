#ifndef NOTEPAD_H
#define NOTEPAD_H

#include <iostream>
#include <fstream>
#include <string>

using namespace std;

void writeNote(string filename);
void readNote(string filename);
void updateNote(string filename);

#endif
